name: CD

on:
  release:
    types:
      - published

jobs:
  cd:
    uses: halo-sigs/reusable-workflows/.github/workflows/plugin-cd.yaml@v4
    permissions:
      contents: write
      packages: write
      id-token: write
    with:
      ui-path: "ui"
      pnpm-version: 10
      node-version: 20
      java-version: 21
      skip-appstore-release: true
